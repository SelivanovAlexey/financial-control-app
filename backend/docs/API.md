# Backend API documentation

![API Version](https://img.shields.io/badge/API-v1.0-blue)

REST API для Financial Control App
Версия: 1.0
Обновлено: 03.02.2026


---

# 📋 Общая информация

## Аутентификация

API использует **session-based аутентификацию** через HTTP cookies:

- После успешного логина/регистрации сервер устанавливает cookie `JSESSIONID`
- Все последующие запросы должны включать эту cookie
- При использовании `rememberMe=true` устанавливается дополнительная cookie на 7 дней
- Cookie автоматически отправляется браузером, для других клиентов нужно передавать вручную

## Формат данных

- **Content-Type:** `application/json`
- **Дата:** ISO 8601 с временной зоной (`2025-01-28T12:00:00+03:00`)
- **Сумма:** `BigDecimal` (число, может быть с дробной частью до 2ух символов, например: `1500.65`)

## Swagger UI

Интерактивная документация API доступна по адресу:

```html
/swagger-ui.html
/scalar
```

---

# 🔐 Аутентификация (Session based)

Все эндпоинты аутентификации находятся под префиксом `/api/auth`.

## 🔑 Логин

Аутентификация существующего пользователя в системе.

**Метод:** `POST`

**URL:** `/api/auth/login`

**Content-Type:** `application/json`

**Требуется аутентификация:** ❌ Нет

### Параметры запроса

| Поле         | Тип       | Обязательное | Описание                                                                    |
|--------------|-----------|--------------|-----------------------------------------------------------------------------|
| `username`   | `string`  | ✅ Да         | Имя пользователя                                                            |
| `password`   | `string`  | ✅ Да         | Пароль                                                                      |
| `rememberMe` | `boolean` | ❌ Нет        | Запомнить пользователя на 7 дней (по умолчанию: `false`)                    |
| `email`      | `string`  | ❌ Нет        | Email (опционально, валидный формат если указан) (сейчас не обрабатывается) |

### Пример запроса

```json
{
  "username": "alex",
  "password": "1234",
  "rememberMe": true
}
```

### Пример ответа (200 OK)

**Headers:**

```html
Set-Cookie: JSESSIONID=E95A8213A8D714C9A07CF615C62379E2; Path=/; HttpOnly;
Set-Cookie: remember-me=YWxleDoxNzUwNjcxMTk2MzM4OlNIQTI1NjplNjMzZDVhNDc2ZmUwNTE1YWRhMzNjYzk3NTZlN2JjZDI3NTE1YWFkNzI0ZTBkMGIwZDVhYTcyNWJlNWRhZWM1; Path=/; HttpOnly; Expires=Mon, 23 Jun 2025 09:33:16 GMT;
```

**Body:** Пустое (или `{}`)

### Возможные ошибки

| Статус             | Описание              | Пример ответа (JSON)                                                                                 |
|--------------------|-----------------------|------------------------------------------------------------------------------------------------------|
| `400 Bad Request`  | Ошибка валидации      | `{"msg":"Validation failed","errors":{"username":"must not be null","password":"must not be null"}}` |
| `401 Unauthorized` | Ошибка аутентификации | `{"msg":"Invalid credentials","cause":"Bad credentials"}`                                            |

> ⚠️ Все последующие запросы к API должны включать сессионную куки JSESSIONID, установленную сервером (remember-me - не
> обязательно). Стереть куки можно посредством логаута.
>

## 🔓 Логаут

Выход из системы и удаление сессионной cookie.

**Метод:** `POST`

**URL:** `/api/auth/logout`

**Требуется аутентификация:** ✅ Да

### Пример ответа (200 OK)

**Headers:**

```
Set-Cookie: JSESSIONID=; Secure; HttpOnly; Path=/; Max-Age=0
Set-Cookie: remember-me=; Secure; HttpOnly; Path=/; Max-Age=0
```

**Body:** Пустое

### Возможные ошибки

| Статус             | Описание                             | Пример ответа (JSON)                                                                                      |
|--------------------|--------------------------------------|-----------------------------------------------------------------------------------------------------------|
| `401 Unauthorized` | Нет активной сессии / не авторизован | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |

> 💡 После выхода из системы, все запросы, использующие прежнюю cookie, будут получать 401 Unauthorized.
>

## 👤 Регистрация

Создание нового пользователя в системе. После успешной регистрации пользователь автоматически авторизуется.

**Метод:** `POST`

**URL:** `/api/auth/signup`

**Content-Type:** `application/json`

**Требуется аутентификация:** ❌ Нет

### Параметры запроса

| Поле              | Тип      | Обязательное | Описание                                             |
|-------------------|----------|--------------|------------------------------------------------------|
| `username`        | `string` | ✅ Да         | Имя пользователя (уникальное)                        |
| `password`        | `string` | ✅ Да         | Пароль                                               |
| `confirmPassword` | `string` | ✅ Да         | Подтверждение пароля (должно совпадать с `password`) |
| `email`           | `string` | ❌ Нет        | Email адрес (валидный формат email)                  |

### Пример запроса

```json
{
  "username": "alex",
  "password": "1234",
  "confirmPassword": "1234",
  "email": "example@gmail.com"
}
```

### Пример ответа (201 Created)

**Body:** Пустое (или `{}`)

**Headers:**

```
Set-Cookie: JSESSIONID=...; Path=/; HttpOnly;
```

### Возможные ошибки

| Статус            | Описание                    | Пример ответа (JSON)                                                                                                                      |
|-------------------|-----------------------------|-------------------------------------------------------------------------------------------------------------------------------------------|
| `400 Bad Request` | Ошибки валидации            | `{"msg":"Validation failed","errors":{"username":"must not be null","password":"must not be null","confirmPassword":"must not be null"}}` |
| `400 Bad Request` | Пароли не совпадают         | `{"msg":"Wrong data","cause":"Passwords don't match"}`                                                                                    |
| `409 Conflict`    | Пользователь уже существует | `{"msg":"User already exists","cause":"User with username 'alex' already exists"}`                                                        |

---

# 💸 CRUD: Расходы

Все эндпоинты для работы с расходами находятся под префиксом `/api/expenses`.

> ⚠️ Требуется аутентификация: Все эндпоинты расходов требуют валидной сессионной cookie.
>

## ➕ Создание расхода

Создание новой записи о расходе для текущего авторизованного пользователя.

**Метод:** `POST`

**URL:** `/api/expenses`

**Content-Type:** `application/json`

**Требуется аутентификация:** ✅ Да

### Параметры запроса

| Поле          | Тип                   | Обязательное | Описание                                       |
|---------------|-----------------------|--------------|------------------------------------------------|
| `amount`      | `number` (BigDecimal) | ✅ Да         | Сумма расхода (например: `1500.23` = 1500.23₽) |
| `category`    | `string`              | ✅ Да         | Категория расхода (максимум 128 символов)      |
| `description` | `string`              | ❌ Нет        | Описание расхода                               |
| `createDate`  | `string` (ISO 8601)   | ✅ Да         | Дата создания                                  |

### Пример запроса

```json
{
  "amount": 1500.25,
  "category": "Food",
  "createDate": "2025-05-28T12:00:00Z",
  "description": "Lunch at cafe"
}
```

### Пример ответа (201 Created)

```json
{
  "id": 1,
  "amount": 1500.25,
  "category": "Food",
  "createDate": "2025-01-28T12:00:00+00:00",
  "description": "Lunch at cafe"
}
```

### Возможные ошибки

| Статус             | Описание                    | Пример ответа (JSON)                                                                                      |
|--------------------|-----------------------------|-----------------------------------------------------------------------------------------------------------|
| `400 Bad Request`  | Ошибка валидации            | `{"msg":"Validation failed","errors":{"amount":"must not be null"}}`                                      |
| `401 Unauthorized` | Пользователь не авторизован | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |

## 📥 Получение всех расходов

Получение списка всех расходов текущего авторизованного пользователя.

**Метод:** `GET`

**URL:** `/api/expenses`

**Требуется аутентификация:** ✅ Да

### Пример ответа (200 OK)

```json
[
  {
    "id": 1,
    "amount": 150000.00,
    "category": "Food",
    "createDate": "2025-01-28T12:00:00+00:00",
    "description": "Lunch at cafe"
  },
  {
    "id": 2,
    "amount": 50000.00,
    "category": "Transport",
    "createDate": "2025-01-28T13:30:00+00:00",
    "description": "Taxi"
  }
]
```

### Возможные ошибки

| Статус             | Описание                    | Пример ответа (JSON)                                                                                      |
|--------------------|-----------------------------|-----------------------------------------------------------------------------------------------------------|
| `401 Unauthorized` | Пользователь не авторизован | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |

> 💡 Примечание: Возвращаются только расходы текущего пользователя. Расходы других пользователей недоступны.
>

## 📄 Получение расхода по id

Получение информации о конкретном расходе по его идентификатору.

**Метод:** `GET`

**URL:** `/api/expenses/{id}`

### Path параметры

| Параметр | Тип             | Обязательное | Описание   |
|----------|-----------------|--------------|------------|
| `id`     | `number` (Long) | ✅ Да         | ID расхода |

### Пример ответа (200 OK)

```json
{
  "id": 1,
  "amount": 150000.00,
  "category": "Food",
  "createDate": "2025-01-28T12:00:00+00:00",
  "description": "Lunch at cafe"
}
```

### Возможные ошибки

| Статус             | Описание                                                  | Пример ответа (JSON)                                                                                      |
|--------------------|-----------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| `401 Unauthorized` | Пользователь не авторизован                               | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |
| `403 Forbidden`    | Доступ запрещён (расход принадлежит другому пользователю) | `{"msg":"Access denied","cause":"Access to this record is not allowed for current user"}`                 |
| `404 Not Found`    | Расход не найден                                          | `{"msg":"Element not found","cause":"ExpenseEntity with id: 999 is not found!"}`                          |

> 💡 Примечание: Возвращаются только расходы текущего пользователя. Расходы других пользователей недоступны.
>

## ♻️ Обновление расхода

Обновление существующего расхода. Можно обновлять только свои расходы.

**Метод:** `PUT`

**URL:** `/api/expenses/{id}`

**Требуется аутентификация:** ✅ Да

### Path параметры

| Параметр | Тип             | Обязательное | Описание   |
|----------|-----------------|--------------|------------|
| `id`     | `number` (Long) | ✅ Да         | ID расхода |

### Параметры запроса

| Поле          | Тип                   | Обязательное | Описание              |
|---------------|-----------------------|--------------|-----------------------|
| `amount`      | `number` (BigDecimal) | ❌ Нет        | Сумма расхода         |
| `category`    | `string`              | ❌ Нет        | Категория расхода     |
| `description` | `string`              | ❌ Нет        | Описание расхода      |
| `createDate`  | `string` (ISO 8601)   | ❌ Нет        | Дата создания расхода |

### Пример запроса

```json
{
  "id": 1,
  "amount": 200000,
  "category": "Food",
  "createDate": "2025-01-28T12:00:00+00:00",
  "description": "Dinner at restaurant"
}
```

### Пример ответа (200 OK)

```json
{
  "id": 1,
  "amount": 200000.00,
  "category": "Food",
  "createDate": "2025-01-28T12:00:00+00:00",
  "description": "Dinner at restaurant"
}
```

### Возможные ошибки

| Статус             | Описание                                                  | Пример ответа (JSON)                                                                                      |
|--------------------|-----------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| `400 Bad Request`  | Ошибка валидации                                          | `{"msg":"Validation failed","errors":{"amount":"must not be null"}}`                                      |
| `401 Unauthorized` | Пользователь не авторизован                               | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |
| `403 Forbidden`    | Доступ запрещён (расход принадлежит другому пользователю) | `{"msg":"Access denied","cause":"Access to this record is not allowed for current user"}`                 |
| `404 Not Found`    | Расход не найден                                          | `{"msg":"Element not found","cause":"ExpenseEntity with id: 999 is not found!"}`                          |

> 💡 Примечание: К изменению доступен только расход текущего пользователя. Расходы других пользователей недоступны.
>

## ❌ Удаление расхода

Удаление расхода по его идентификатору.

**Метод:** `DELETE`

**URL:** `/api/expenses/{id}`

**Требуется аутентификация:** ✅ Да

### Path параметры

| Параметр | Тип             | Обязательное | Описание   |
|----------|-----------------|--------------|------------|
| `id`     | `number` (Long) | ✅ Да         | ID расхода |

### Пример ответа (204 No Content)

**Body:** Пустое

### Возможные ошибки

| Статус             | Описание                                                  | Пример ответа (JSON)                                                                                      |
|--------------------|-----------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| `401 Unauthorized` | Пользователь не авторизован                               | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |
| `403 Forbidden`    | Доступ запрещён (расход принадлежит другому пользователю) | `{"msg":"Access denied","cause":"Access to this record is not allowed for current user"}`                 |
| `404 Not Found`    | Расход не найден                                          | `{"msg":"Element not found","cause":"ExpenseEntity with id: 999 is not found!"}`                          |

> 💡 Примечание: К удалению доступен только расход текущего пользователя. Расходы других пользователей недоступны.
>

---

# 💰 CRUD: Доходы

Все эндпоинты для работы с доходами находятся под префиксом `/api/incomes`.

> ⚠️ Требуется аутентификация: Все эндпоинты доходов требуют валидной сессионной cookie.
>

## ➕ Создание дохода

Создание новой записи о доходе для текущего авторизованного пользователя.

**Метод:** `POST`

**URL:** `/api/incomes`

**Content-Type:** `application/json`

**Требуется аутентификация:** ✅ Да

### Параметры запроса

| Поле          | Тип                   | Обязательное | Описание                                          |
|---------------|-----------------------|--------------|---------------------------------------------------|
| `amount`      | `number` (BigDecimal) | ✅ Да         | Сумма дохода (например: `354443.65` = 354443.65₽) |
| `category`    | `string`              | ✅ Да         | Категория дохода (максимум 128 символов)          |
| `description` | `string`              | ❌ Нет        | Описание дохода                                   |
| `createDate`  | `string` (ISO 8601)   | ✅ Да         | Дата создания                                     |

### Пример запроса

```json
{
  "amount": 3000.12,
  "category": "Salary",
  "createDate": "2025-05-28T12:00:00Z",
  "description": "Monthly salary"
}
```

### Пример ответа (201 Created)

```json
{
  "id": 1,
  "amount": 3000.12,
  "category": "Salary",
  "createDate": "2025-01-28T12:00:00+00:00",
  "description": "Monthly salary"
}
```

### Возможные ошибки

| Статус             | Описание                    | Пример ответа (JSON)                                                                                      |
|--------------------|-----------------------------|-----------------------------------------------------------------------------------------------------------|
| `400 Bad Request`  | Ошибка валидации            | `{"msg":"Validation failed","errors":{"amount":"must not be null"}}`                                      |
| `401 Unauthorized` | Пользователь не авторизован | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |

## 📥 Получение всех доходов

Получение списка всех доходов текущего авторизованного пользователя.

**Метод:** `GET`

**URL:** `/api/incomes`

**Требуется аутентификация:** ✅ Да

### Пример ответа (200 OK)

```json
[
  {
    "id": 1,
    "amount": 3000.12,
    "category": "Salary",
    "createDate": "2025-01-28T12:00:00+00:00",
    "description": "Monthly salary"
  },
  {
    "id": 2,
    "amount": 5000.65,
    "category": "Freelance",
    "createDate": "2025-01-28T14:00:00+00:00",
    "description": "Project payment"
  }
]
```

### Возможные ошибки

| Статус             | Описание                    | Пример ответа (JSON)                                                                                      |
|--------------------|-----------------------------|-----------------------------------------------------------------------------------------------------------|
| `401 Unauthorized` | Пользователь не авторизован | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |

> 💡 Примечание: Возвращаются только доходы текущего пользователя. Доходы других пользователей недоступны.
>

## 📄 Получение дохода по id

Получение информации о конкретном доходе по его идентификатору.

**Метод:** `GET`

**URL:** `/api/incomes/{id}`

**Требуется аутентификация:** ✅ Да

### Path параметры

| Параметр | Тип             | Обязательное | Описание  |
|----------|-----------------|--------------|-----------|
| `id`     | `number` (Long) | ✅ Да         | ID дохода |

### Пример ответа (200 OK)

```json
{
  "id": 1,
  "amount": 3000.00,
  "category": "Salary",
  "createDate": "2025-01-28T12:00:00+00:00",
  "description": "Monthly salary"
}
```

### Возможные ошибки

| Статус             | Описание                                                 | Пример ответа (JSON)                                                                                      |
|--------------------|----------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| `401 Unauthorized` | Пользователь не авторизован                              | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |
| `403 Forbidden`    | Доступ запрещён (доход принадлежит другому пользователю) | `{"msg":"Access denied","cause":"Access to this record is not allowed for current user"}`                 |
| `404 Not Found`    | Доход не найден                                          | `{"msg":"Element not found","cause":"IncomeEntity with id: 999 is not found!"}`                           |

> 💡 Примечание: Возвращаются только доходы текущего пользователя. Доходы других пользователей недоступны.
>

## ♻️ Обновление дохода по id

Обновление существующего дохода. Можно обновлять только свои доходы.

**Метод:** `PUT`

**URL:** `/api/incomes/{id}`

**Требуется аутентификация:** ✅ Да

### Path параметры

| Параметр | Тип             | Обязательное | Описание  |
|----------|-----------------|--------------|-----------|
| `id`     | `number` (Long) | ✅ Да         | ID дохода |

### Параметры запроса

| Поле          | Тип                   | Обязательное | Описание             |
|---------------|-----------------------|--------------|----------------------|
| `amount`      | `number` (BigDecimal) | ❌ Нет        | Сумма дохода         |
| `category`    | `string`              | ❌ Нет        | Категория дохода     |
| `description` | `string`              | ❌ Нет        | Описание дохода      |
| `createDate`  | `string` (ISO 8601)   | ❌ Нет        | Дата создания дохода |

### Пример запроса

```json
{
  "id": 1,
  "amount": 3500000,
  "category": "Salary",
  "createDate": "2025-01-28T12:00:00+00:00",
  "description": "Monthly salary with bonus"
}
```

### Пример ответа (200 OK)

```json
{
  "id": 1,
  "amount": 3500000.00,
  "category": "Salary",
  "createDate": "2025-01-28T12:00:00+00:00",
  "description": "Monthly salary with bonus"
}
```

### Возможные ошибки

| Статус             | Описание                                                 | Пример ответа (JSON)                                                                                      |
|--------------------|----------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| `400 Bad Request`  | Ошибка валидации                                         | `{"msg":"Validation failed","errors":{"amount":"must not be null"}}`                                      |
| `401 Unauthorized` | Пользователь не авторизован                              | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |
| `403 Forbidden`    | Доступ запрещён (доход принадлежит другому пользователю) | `{"msg":"Access denied","cause":"Access to this record is not allowed for current user"}`                 |
| `404 Not Found`    | Доход не найден                                          | `{"msg":"Element not found","cause":"IncomeEntity with id: 999 is not found!"}`                           |

> 💡 Примечание: К изменению доступен только доход текущего пользователя. Доходы других пользователей недоступны.
>

## ❌ Удаление дохода по id

Удаление дохода по его идентификатору.

**Метод:** `DELETE`

**URL:** `/api/incomes/{id}`

### Path параметры

| Параметр | Тип             | Обязательное | Описание  |
|----------|-----------------|--------------|-----------|
| `id`     | `number` (Long) | ✅ Да         | ID дохода |

### Пример ответа (204 No content)

**Body:** Пустое

### Возможные ошибки

| Статус             | Описание                                                 | Пример ответа (JSON)                                                                                      |
|--------------------|----------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| `401 Unauthorized` | Пользователь не авторизован                              | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |
| `403 Forbidden`    | Доступ запрещён (доход принадлежит другому пользователю) | `{"msg":"Access denied","cause":"Access to this record is not allowed for current user"}`                 |
| `404 Not Found`    | Доход не найден                                          | `{"msg":"Element not found","cause":"IncomeEntity with id: 999 is not found!"}`                           |

> 💡 Примечание: К удалению доступен только доход текущего пользователя. Доходы других пользователей недоступны.
>

---

# 👤 Управление профилем

Все эндпоинты для работы с информацией о пользователе находятся под префиксом `/api/users`.

> ⚠️ Требуется аутентификация: Все эндпоинты профиля требуют валидной сессионной cookie.
>

## 🖋️ Получение информации о пользователе

Получение детальной информации о текущем авторизованном пользователе.

**Метод:** `GET`

**URL:** `/api/users/me`

**Content-Type:** `application/json`

**Требуется аутентификация:** ✅ Да

### Пример ответа (200 OK)

```json
{
  "id": 456,
  "username": "johndoe",
  "displayName": "John Doe",
  "email": "john.doe@example.com"
}
```

### Возможные ошибки

| Статус                      | Описание                                                                  | Пример ответа (JSON)                                                                                      |
|-----------------------------|---------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| `401 Unauthorized`          | Пользователь не авторизован                                               | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |
| `403 Forbidden`             | Доступ запрещён (при попытке запросить пользователя не из текущей сессии) | `{"msg":"Access denied","cause":"Access to this record is not allowed for current user"}`                 |
| `405 Method Not Allowed`    | Нет реализации запрашиваемого метода                                      | `{"msg":"There is no such http method","cause":"Request method 'POST' is not supported"}`                 |
| `500 Internal Server Error` | Внутренняя ошибка сервера                                                 | `{"msg":"Something went wrong","cause":"Detailed error cause"}`                                           |

## 🐥 Создание пользователя

Реализация отсутствует.

## 📄 Обновление информации о пользователе

Обновление информации пользователя.

**Метод:** `PATCH`

**URL:** `/api/users/me`

**Требуется аутентификация:** ✅ Да

### Параметры запроса

| Поле              | Тип      | Обязательное | Описание                                                                         |
|-------------------|----------|--------------|----------------------------------------------------------------------------------|
| `password`        | `string` | ❌ Нет        | Новый пароль                                                                     |
| `confirmPassword` | `string` | ❌ Нет        | Подтверждение пароля (проверяется только когда отправляется вместе с `password`) |
| `displayName`     | `string` | ❌ Нет        | Отображаемое имя                                                                 |
| `email`           | `string` | ❌ Нет        | Почта                                                                            |

### Пример запроса

```json
{
  "password": "newpassword123",
  "confirmPassword": "newpassword123",
  "displayName": "Updated Name",
  "email": "newemail@example.com"
}
```

### Пример ответа (200 OK)

```json
{
  "id": 456,
  "username": "johndoe",
  "displayName": "Updated Name",
  "email": "newemail@example.com"
}
```

### Возможные ошибки

| Статус                      | Описание                                                                 | Пример ответа (JSON)                                                                                      |
|-----------------------------|--------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| `400 Bad Request`           | Ошибки валидации                                                         | `{"msg":"Validation failed","errors":{"password":"must not be null"}}`                                    |
| `401 Unauthorized`          | Пользователь не авторизован                                              | `{"msg":"Insufficient authentication","cause":"Full authentication is required to access this resource"}` |
| `403 Forbidden`             | Доступ запрещён (при попытке обновить пользователя не из текущей сессии) | `{"msg":"Access denied","cause":"Access to this record is not allowed for current user"}`                 |
| `405 Method Not Allowed`    | Нет реализации запрашиваемого метода                                     | `{"msg":"There is no such http method","cause":"Request method 'POST' is not supported"}`                 |
| `500 Internal Server Error` | Внутренняя ошибка сервера                                                | `{"msg":"Something went wrong","cause":"Detailed error cause"}`                                           |

## ❌ Удаление пользователя

Реализация отсутствует.

---

# ⚠️ Обработка ошибок

## Общие форматы ответов об ошибках

### ValidationExceptionJson (400 Bad Request)

Используется при ошибках валидации входных данных.

**Структура:**

```json
{
  "msg": "Validation failed",
  "errors": {
    "fieldName": "error message",
    "username": "must not be null",
    "email": "must be a well-formed email address"
  }
}
```

### CommonExceptionJson (401, 403, 404, 405, 409, 500)

Используется для остальных типов ошибок.

**Структура:**

```json
{
  "msg": "Error message",
  "cause": "Detailed error cause"
}
```

## Коды статусов HTTP

| Код   | Название              | Описание                  | Когда возникает                                               |
|-------|-----------------------|---------------------------|---------------------------------------------------------------|
| `200` | OK                    | Успешный запрос           | Операция выполнена успешно                                    |
| `400` | Bad Request           | Неверный запрос           | Ошибки валидации, неверные логин/пароль, несовпадение паролей |
| `401` | Unauthorized          | Не авторизован            | Отсутствует или недействительная сессия                       |
| `403` | Forbidden             | Доступ запрещён           | Попытка обновить чужую запись                                 |
| `404` | Not Found             | Не найдено                | Запрашиваемый ресурс не существует                            |
| `405` | Method Not Allowed    | Метод не доступен         | Нет реализации данной операции                                |
| `409` | Conflict              | Конфликт данных           | Пользователь уже существует, нарушение целостности данных     |
| `500` | Internal Server Error | Внутренняя ошибка сервера | Непредвиденная ошибка, необработанные исключения              |

---

# 🎯 Сценарии использования

## Сценарий 1: Первый запуск приложения

**Цель:** Зарегистрировать нового пользователя и создать первую транзакцию

**Шаги:**

1. **Регистрация пользователя**

    ```
    POST /api/auth/signup
    Content-Type: application/json
    
    {
      "username": "ivan",
      "password": "secure123",
      "confirmPassword": "secure123",
      "email": "ivan@example.com"
    }
    ```

   → Получаем cookie `JSESSIONID` в заголовках ответа

2. **Создание первого дохода (зарплата)**

    ```
    POST /api/incomes
    Cookie: JSESSIONID=...
    Content-Type: application/json
    
    {
      "amount": 10000.00,
      "category": "Salary",
      "description": "Зарплата за январь"
    }
    ```

3. **Добавление первого расхода**

    ```
    POST /api/expenses
    Cookie: JSESSIONID=...
    Content-Type: application/json
    
    {
      "amount": 5000.00,
      "category": "Food",
      "description": "Продукты на неделю"
    }
    ```

4. **Получение баланса**

    ```
    GET /api/incomes
    Cookie: JSESSIONID=...
    
    GET /api/expenses
    Cookie: JSESSIONID=...
    ```

   → Рассчитываем баланс на клиенте: сумма доходов - сумма расходов

## Сценарий 2: Ежедневное использование

**Цель:** Быстро добавить несколько расходов за день

**Шаги:**

1. **Логин (если cookie истекла)**

    ```
    POST /api/auth/login
    Content-Type: application/json
    
    {
      "username": "ivan",
      "password": "secure123",
      "rememberMe": true
    }
    ```

   → Если `rememberMe=true`, cookie действует 7 дней

2. **Добавление расходов в течение дня**

    ```
    POST /api/expenses
    Cookie: JSESSIONID=...
    Content-Type: application/json
    
    {
      "amount": 50000,
      "category": "Transport",
      "description": "Такси до офиса"
    }
    
    POST /api/expenses
    Cookie: JSESSIONID=...
    Content-Type: application/json
    
    {
      "amount": 300000,
      "category": "Food",
      "description": "Обед в ресторане"
    }
    ```

3. **Просмотр статистики за день**

    ```
    GET /api/expenses
    Cookie: JSESSIONID=...
    ```

   → Фильтруем по дате на клиенте и группируем по категориям

## Сценарий 3: Исправление ошибки

**Цель:** Обновить сумму расхода, если была допущена ошибка

**Шаги:**

1. **Получение расхода по ID**

    ```
    GET /api/expenses/5
    Cookie: JSESSIONID=...
    ```

   → Получаем: `{ "id": 5, "amount": 100000, "category": "Food", ... }`

2. **Обновление суммы**

    ```
    PUT /api/expenses/5
    Cookie: JSESSIONID=...
    Content-Type: application/json
    
    {
      "amount": 150000,
      "category": "Food",
      "description": "Обед"
    }
    ```

   → Исправляем сумму с 1000₽ на 1500₽

## Сценарий 4: Анализ расходов по категориям

**Цель:** Получить все расходы и сгруппировать по категориям для анализа

**Шаги:**

1. **Получение всех расходов**

    ```
    GET /api/expenses
    Cookie: JSESSIONID=...
    ```

2. **Ответ:**

    ```json
    [
        {
            "id": 1,
            "amount": 150000.00,
            "category": "Food"
        },
        {
            "id": 2,
            "amount": 50000.00,
            "category": "Transport"
        },
        {
            "id": 3,
            "amount": 200000.00,
            "category": "Food"
        }
    ]
    ```

3. **Группировка на клиенте:**
    - Food: 350000 (3500₽)
    - Transport: 50000 (500₽)

## Сценарий 5: Удаление ошибочной записи

**Цель:** Удалить случайно созданную транзакцию

**Шаги:**

1. **Удаление расхода**

    ```
    DELETE /api/expenses/10
    Cookie: JSESSIONID=...
    ```

   → `200 OK`

2. **Проверка удаления**

    ```
    GET /api/expenses/10
    Cookie: JSESSIONID=...
    ```

   → `404 Not Found` 
